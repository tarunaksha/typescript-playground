// const superHeros: string[] = []
// superHeros.push("iron man")

// const superHeros: Array<string> = []
// superHeros.push("spoidermonn")

type User = {
  name: string;
  isActive: boolean;
};
const allUsers: User[] = [];
allUsers.push(
  { name: "tarun", isActive: false },
  { name: "neha", isActive: true }
);
